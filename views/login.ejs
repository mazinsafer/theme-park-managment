<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <style>
        body {
            font-family: sans-serif;
            margin: 0;
            background-color: #f4f4f9;
            color: #333;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }
        form {
            max-width: 400px;
            width: 100%;
            background: white;
            padding: 2.5em;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }
        h1 {
            color: #444;
            text-align: center;
            margin-top: 0;
            margin-bottom: 1.5em;
            border-bottom: 2px solid #eee;
            padding-bottom: 0.5em;
        }
        div {
            margin-bottom: 1.5em;
        }
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }
        input[type="text"], input[type="password"] {
            width: 100%;
            padding: 12px;
            box-sizing: border-box;
            border-radius: 4px;
            border: 1px solid #ccc;
        }
        .error {
            color: #d9534f;
            font-weight: bold;
            background-color: #f2dede;
            padding: 1em;
            border-radius: 4px;
            margin-bottom: 1.5em;
            text-align: center;
        }
        button {
            width: 100%;
            padding: 12px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            font-size: 1.1em;
            font-weight: bold;
        }
        button:hover {
            background-color: #0056b3;
        }
        
        .info-note {
            text-align: center;
            font-size: 0.9em;
            color: #6c757d;
            margin-top: 1.5em;
            margin-bottom: 0;
        }
    </style>
</head>
<body>
    
    <form action="/login" method="POST">
        <h1>Park Management Login</h1>

        <% if (typeof error !== 'undefined' && error) { %>
            <p class="error"><%= error %></p>
        <% } %>

        <div>
            <label for="username">Email (Username):</label>
            <input type="text" id="username" name="username" required>
        </div>
        
        <div>
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" required>
        </div>
        
        <button type="submit">Login</button>

        <p class="info-note">
            Please contact HR or an Admin to reset password
        </p>

    </form>
</body>
</html>