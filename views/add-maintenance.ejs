<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Report Maintenance Issue</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f9; color: #333; }
        h1 { color: #444; text-align: center; margin-top: 1em; }
        h1 small { color: #dc3545; font-weight: bold; }
        form { max-width: 500px; margin: 1em auto 2em auto; background: white; padding: 2em; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        div { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; font-weight: bold; }
        textarea, select {
            width: 100%; padding: 10px; box-sizing: border-box; border-radius: 4px; border: 1px solid #ccc;
        }
        textarea { resize: vertical; min-height: 100px; }
        .error { color: #d9534f; font-weight: bold; background-color: #f2dede; padding: 1em; border-radius: 4px; margin-bottom: 15px; }
        button { padding: 12px 20px; background-color: #dc3545; color: white; border: none; cursor: pointer; border-radius: 4px; font-size: 1em; }
        button:hover { background-color: #c82333;
 }
        
        .cancel-link {
            display: inline-block;
            margin-left: 15px;
            text-decoration: none;
            color: #6c757d;
            font-weight: bold;
        }
        .cancel-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <h1>Report Issue for: <small><%= ride.ride_name %></small></h1>

    <% if (typeof error !== 'undefined' && error) { %>
        <p class="error"><%= error %></p>
    <% } %>

    <form action="/maintenance" method="POST">
        <input type="hidden" name="ride_id" value="<%= ride.ride_id %>">
        
        <div>
            <label for="summary">Issue Summary:</label>
            <textarea id="summary" name="summary" required></textarea>
        </div>
        <div>
            <label for="employee_id">Assign To (optional):</label>
            <select id="employee_id" name="employee_id">
                <option value="">-- Assign Later --</option>
                <% employees.forEach(employee => { %>
                    <option value="<%= employee.employee_id %>"><%= employee.first_name %> <%= employee.last_name %></option>
                <% }); %>
            </select>
        </div>
        <br>
        <button type="submit">Submit Report</button>
        <a href="/rides" class="cancel-link">Cancel</a>
    </form>
</body>
</html>
