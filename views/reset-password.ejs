<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Reset Employee Password</title>
    <style>
        body {
            font-family: sans-serif;
            margin: 0;
            background-color: #f4f4f9;
            color: #333;
        }
        form {
            max-width: 450px;
            margin: 2em auto;
            background: white;
            padding: 2.5em;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }
        h1 {
            color: #444;
            text-align: center;
            margin-top: 0;
            margin-bottom: 0.5em;
        }
        h1 small {
            color: #dc3545;
            font-size: 0.7em;
            display: block;
            font-weight: normal;
        }
        div {
            margin-bottom: 1.5em;
        }
        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
        }
        input[type="password"] {
            width: 100%;
            padding: 12px;
            box-sizing: border-box;
            border-radius: 4px;
            border: 1px solid #ccc;
        }
        .error {
            color: #d9534f;
            font-weight: bold;
            background-color: #f2dede;
            padding: 1em;
            border-radius: 4px;
            margin-bottom: 1.5em;
            text-align: center;
        }
        button {
            width: 100%;
            padding: 12px 20px;
            background-color: #dc3545;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 4px;
            font-size: 1.1em;
            font-weight: bold;
        }
        button:hover {
            background-color: #c82333;
        }

        /* --- NEW 'CANCEL' LINK STYLE --- */
        .cancel-link {
            display: block;
            text-align: center;
            margin-top: 1em;
            text-decoration: none;
            color: #6c757d;
            font-weight: bold;
        }
        .cancel-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <form action="/employees/reset-password/<%= employee.employee_id %>" method="POST">
        <h1>Reset Password
            <small>For: <%= employee.first_name %> <%= employee.last_name %></small>
        </h1>

        <% if (typeof error !== 'undefined' && error) { %>
            <p class="error"><%= error %></p>
        <% } %>

        <div>
            <label for="password">New Temporary Password:</label>
            <input type="password" id="password" name="password" required>
        </div>
        
        <div>
            <label for="confirm_password">Confirm New Password:</label>
            <input type="password" id="confirm_password" name="confirm_password" required>
        </div>
        
        <button type="submit">Set New Password</button>

        <a href="/users" class="cancel-link">Cancel</a>
    </form>
</body>
</html>