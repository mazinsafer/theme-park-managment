<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Park Members</title>
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f9; color: #333; }
        .container { max-width: 1000px; margin: 2em auto; background: white; padding: 2em; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
        h1 { color: #444; border-bottom: 2px solid #eee; padding-bottom: 0.5em; }
        table { width: 100%; border-collapse: collapse; margin-top: 1.5em; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background-color: #17a2b8; color: white; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        tr:hover { background-color: #f1f1f1; }
        .add-button { display: inline-block; margin-bottom: 1.5em; padding: 12px 20px; background-color: #28a745; color: white; text-decoration: none; border-radius: 4px; font-weight: bold; }
        .add-button:hover { background-color: #218838; }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <h1>Park Members</h1>
        <a href="/members/new" class="add-button">Add New Member</a>

        <form action="/members/search" method="POST">
            <input type="text" name="searchTerm" placeholder="Search by Email" value="<%= searchTerm %>">
            <button type="submit">Search</button>
        </form>
        <table>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Member Type</th>
                    <th>Start Date</th>
                    <th>End Date</th>
                </tr>
            </thead>
            <tbody>
                <% if (members.length > 0) { %>
                    <% members.forEach(member => { %>
                        <tr>
                            <td><%= member.first_name %> <%= member.last_name %></td>
                            <td><%= member.email %></td>
                            <td><%= member.phone_number %></td>
                            <td><%= member.member_type %></td>
                            <td><%= member.start_date.toISOString().substring(0, 10) %></td>
                            <td><%= member.end_date.toISOString().substring(0, 10) %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <p>No results found.</p>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
